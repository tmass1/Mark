{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "selector",
  "description": "The selection overlay may take a region, cancel, and tell its siblings to clear.",
  "windows": ["selector-*"], "platforms": ["macOS"],
  "permissions": [
    "allow-capture-rect", "allow-cancel-selection",
    "core:event:allow-listen", "core:event:allow-unlisten", "core:event:allow-emit"
  ]
}
